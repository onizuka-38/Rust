# hello_rust

Rust 프로젝트의 가장 기본적인 실행 구조를 확인하는 작은 예제입니다.

## 구조

- `src/main.rs`: 프로그램 진입점
- `src/lesson1.rs`: 첫 번째 모듈 예제

현재 `main.rs`는 `lesson1::run()`을 호출합니다.

## 실행

```powershell
cd hello_rust
cargo run
```
