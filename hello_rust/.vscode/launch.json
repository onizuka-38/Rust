{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (Rust)",
      "type": "lldb",
      "request": "launch",
      "cargo": { "args": ["build"] },
      "program": "${cargo:program}",
      "cwd": "${workspaceFolder}",
      "terminal": "integrated"
    }
  ]
}
